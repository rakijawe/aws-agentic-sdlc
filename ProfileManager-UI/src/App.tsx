import React from 'react';
import { BrowserRouter } from 'react-router-dom';
import { Container } from '@mui/material';

function App() {
  return (
    <BrowserRouter>
      <Container>
        <h1>User Authentication System</h1>
        <p>Project structure created. Ready for implementation.</p>
      </Container>
    </BrowserRouter>
  );
}

export default App;
